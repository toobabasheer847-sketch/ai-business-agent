/**
 * @deprecated Use `phoneNumbers` — Twilio fields are on the same table now.
 * Kept as a re-export alias for gradual import cleanup.
 */
export { phoneNumbers as twilioApps } from './phone-number.schema';
